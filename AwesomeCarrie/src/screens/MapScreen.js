import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const MapScreen = () => {
  return (
    <View>
      <Text>This is map staff....</Text>
    </View>
  );
};

export default MapScreen;

const styles = StyleSheet.create({});
